# This Workflow is DISABLED for pezkuwichain/pezkuwi-sdk.
# It was originally for paritytech-release fork synchronization.
# Pezkuwi SDK is an independent project and doesn't need upstream sync.

name: Sync the forked repo with the upstream (DISABLED)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled for Pezkuwi SDK'
        required: false
        default: 'disabled'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled for Pezkuwi SDK - independent blockchain project"
